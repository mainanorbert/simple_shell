#ifndef MIAN_H
#define MIAN_H
int _putchar(char);
int _printf(const char *format, ...);
int _strscat(char *);
int number(int a);
int bin(unsigned int);
#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>
#include <limits.h>
#endif
